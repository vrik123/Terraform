pipeline{
	agent any
	environment {
	  PATH = "${PATH}:${getTerrPath()}"
	  }
	stages{
	  stage ('Terraform-Init'){
	    steps{
	      sh "terraform init"
	         }
	    }
	   stage ('Terraform-Plan'){
	   steps{
	      sh "terraform apply  -var-file="
	        }
	   }
	}
}
def getTerrPath(){
def tfHome = tool name: 'Terraform-12', type: 'terraform'
return tfHome
}
