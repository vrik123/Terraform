pipeline{
	agent any
	environment {
	  PATH = "${PATH}:${getTerrPath()}"
	  }
	stages{
	  stage ('Terraform-Init'){
	    steps{
	      sh "terraform init"
	         }
	    }
	   stage ('Terraform-Init'){
	   steps{
	      sh "terraform plan"
	        }
	   }
	}
}
def getTerrPath(){
def tfHome = tool name: 'Terraform-12', type: 'terraform'
return tfHome
}
